{
  "name": "@aerobeat/web-cv-testbed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "demo": "echo \"Add a repo-local Vite or static demo command when CV scenes need serving.\"",
    "serve": "echo \"Define host, port, cache busting, QR/link output, served roots, and secure-context handling before live camera validation.\"",
    "test": "echo \"Add CV service testbed tests when frame sources land.\"",
    "test:browser": "echo \"Add Playwright browser tests for live, video, and replay scenes.\""
  }
}
